import { FastifyInstance } from "fastify";
import {  } from "./cartItems.controller";
import { $ref } from "./cartItems.schema";

async function cartRoutes(server: FastifyInstance) {

    
}

export default cartRoutes